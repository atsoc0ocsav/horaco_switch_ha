{
  "config": {
    "step": {
      "user": {
        "title": "Connect to your Managed Switch",
        "description": "Works with HORACO HC-SWTGW218AS, HC-SWTGW215AS, ZX-SWTG124AS, keepLink KP9000 and compatible OEM switches. Default credentials are admin / admin.",
        "data": {
          "host": "Switch IP Address",
          "port": "HTTP Port",
          "username": "Username",
          "password": "Password",
          "scan_interval": "Polling interval (seconds)"
        },
        "data_description": {
          "scan_interval": "How often to poll the switch, 10 to 300 seconds. Lower values react faster but load the switch's small web server harder; 30 seconds is a good default. You can change this later without re-adding the switch."
        }
      }
    },
    "error": {
      "cannot_connect": "Cannot reach the switch. Check the IP address and credentials.",
      "unknown": "Unexpected error — check the Home Assistant logs."
    },
    "abort": {
      "already_configured": "This switch is already configured."
    }
  },
  "options": {
    "step": {
      "init": {
        "title": "HORACO Switch Options",
        "data": {
          "scan_interval": "Polling interval (seconds)",
          "assumed_tx_frame_bytes": "Assumed average TX frame size (bytes)",
          "assumed_rx_frame_bytes": "Assumed average RX frame size (bytes)"
        },
        "data_description": {
          "scan_interval": "How often to poll the switch, 10 to 300 seconds. Changes apply immediately — no restart needed.",
          "assumed_tx_frame_bytes": "Leave at 0 to disable. The switch counts frames, not bytes, so a bit rate can only be estimated by assuming an average frame size. Measure it on the device at the other end of the link if it reports byte counters: bytes divided by packets. The two directions usually differ a lot — on a real uplink one averaged 1372 B while the other averaged 239 B — which is why they are set separately. A wrong value is wrong proportionally: assuming 1518 B where the truth was 854 B overstated the rate by 80%.",
          "assumed_rx_frame_bytes": "Same as above, for the receive direction. Set it independently."
        }
      }
    }
  }
}
